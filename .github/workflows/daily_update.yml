name: Jason TV Daily Auto Update
on:
  schedule:
    - cron: '30 1 * * *'
  workflow_dispatch:
  
permissions:
  contents: write
  
jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 下載程式碼
        uses: actions/checkout@v4
      
      - name: 🐍 設定 Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          
      - name: 📦 安裝套件
        run: |
          pip install --upgrade pip
          pip install requests yfinance pandas lxml google-generativeai
          
      - name: ▶️ 執行腳本
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          YOUTUBE_API_KEY: ${{ secrets.YOUTUBE_API_KEY }}
        run: python update.py
        
      - name: 🔍 檢查結果
        run: |
          if [ -f "index.html" ]; then
            echo "✅ index.html 已生成 ($(wc -c < index.html) bytes)"
            head -30 index.html
          else
            echo "❌ index.html 未生成"
            exit 1
          fi
          
      - name: 📤 提交更新
        run: |
          git config user.name "Jason-TV-Bot"
          git config user.email "bot@jasontv.ai"
          git add index.html
          git diff --staged --quiet || git commit -m "🤖 Auto update: $(date '+%Y-%m-%d %H:%M')"
          git push
